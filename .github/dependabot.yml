version: 2
updates:
  # Keep the GitHub Actions used in .github/workflows/ patched.
  # This repo ships no package manifests, so github-actions is the only ecosystem.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
