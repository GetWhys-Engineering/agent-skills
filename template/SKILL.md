---
name: my-skill-name
description: One or two sentences describing what this skill does. Lead with the use case and the trigger phrases a user would say, so clients know when to load it.
---

# My Skill Name

Replace this body with the skill's instructions.

<!--
Authoring rules (enforced by scripts/validate-skills.sh — see AGENTS.md):
- `name`: kebab-case (lowercase letters, numbers, hyphens), max 64 chars,
  and it MUST match this skill's directory name under skills/.
- `description`: non-empty, max 1024 chars.
- No XML/HTML tags (<...>) in `name` or `description`.
- Only the two standard frontmatter keys above — no vendor-specific keys —
  so the skill stays portable across all SKILL.md-compatible clients.
- Supporting files (scripts/, references/, etc.) are allowed alongside
  SKILL.md, but keep the skill directory under 200 files total.
-->
